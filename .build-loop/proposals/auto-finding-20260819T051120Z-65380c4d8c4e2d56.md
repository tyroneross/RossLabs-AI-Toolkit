---
id: 65380c4d8c4e2d56
kind: finding
route: review
severity: unknown
finding_hash: 65380c4d8c4e2d56
source: auto-finding-sweep
agent: session
source_kind: tool_result
tier: 1-deterministic
title: "| SEC-002 | HIGH | recall.py addmetafilter and addmetainfilter now enforce a frozenset allowlist on field before f-string interpolation. Future callers passing "
captured_at: 2026-08-19T05:11:20.825424+00:00
---

## Finding (no severity asserted — needs human triage)

> | SEC-002 | HIGH | `recall.py` `_add_meta_filter` and `_add_meta_in_filter` now enforce a frozenset allowlist on `field` before f-string interpolation. Future callers passing dynamic input will raise `ValueError` instead of silently allowing SQL injection. | `de17a72` |

## Why this is in the review queue

An agent surfaced a finding-shaped statement but did NOT tag it with a severity, so the auto-sweep routed it here instead of straight to the backlog (high precision over recall).

## Next action

Confirm + promote to the backlog with a severity:

```bash
python3 scripts/backlog.py new --repo . --area audit --type fix \
  --title "| SEC-002 | HIGH | recall.py addmetafilter and addmetainfilter now enforce a frozenset allowlist on field before f-string interpolation. Future callers passing " --priority P2 \
  --provenance-source auto-finding-sweep --provenance-ref finding-hash:65380c4d8c4e2d56
```

...or delete this file if it is not a real finding.
