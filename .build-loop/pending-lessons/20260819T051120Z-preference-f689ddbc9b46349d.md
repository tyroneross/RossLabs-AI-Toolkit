---
id: f689ddbc9b46349d
kind: preference
signal_type: never
confidence: confirmed
scope: global
turn_index: 30
captured_chars: 39
tier: 1-deterministic
source: stop-hook
title: 'never `when_to_use` — confirmed by grep'
captured_at: 2026-08-19T05:11:20.814135+00:00
---

## Quote

> never `when_to_use` — confirmed by grep

## Context (±200 chars)

```
de, and it's false for routing: an agent picks a skill *from* the row, it doesn't click through 50 files first to disambiguate.
- `scripts/skill_index.py:261`: `build_row()` reads only `description`, never `when_to_use` — confirmed by grep, zero hits.

**Escape path** (why no test caught it) — I independently re-verified both of Fable's claims here, since they're the ones that change the recommendation:
- `scripts/test_skill_index.py`
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
