---
id: ee5d43daa7e34185
kind: preference
signal_type: never
confidence: confirmed
scope: global
turn_index: 30
captured_chars: 45
tier: 1-deterministic
source: stop-hook
title: 'never looks at the rendered `docs/SKILL-INDEX'
captured_at: 2026-08-19T05:11:20.814518+00:00
---

## Quote

> never looks at the rendered `docs/SKILL-INDEX

## Context (±200 chars)

```
rigger phrases, in the same file, with no assertion that anything disambiguates them. I also confirmed (read the file header) this test only checks the **source frontmatter** via substring match — it never looks at the rendered `docs/SKILL-INDEX.md` at all. So the one test in the repo whose job is "does this skill's description cover its triggers" is structurally blind to the exact collision it's sitting on top of.
- `--check` (the drift gua
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
