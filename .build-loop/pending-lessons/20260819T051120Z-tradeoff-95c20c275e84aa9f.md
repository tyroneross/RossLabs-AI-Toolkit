---
id: 95c20c275e84aa9f
kind: tradeoff
signal_type: instead_of
confidence: confirmed
scope: global
turn_index: 8
captured_chars: 125
tier: 1-deterministic
source: stop-hook
title: 'dropped from the literal to `None` so an unflagged CLI call reaches the resolver'
captured_at: 2026-08-19T05:11:20.807591+00:00
---

## Quote

> dropped from the literal to `None` so an unflagged CLI call reaches the resolver instead of pre-filling the maintainer's path

## Context (±200 chars)

```
s` now pulls `memory_store_root`, and `append_milestone()` resolves `memory_root_path` as `Path(expanduser(memory_root)) if memory_root else memory_store_root()`. The `--memory-root` argparse default dropped from the literal to `None` so an unflagged CLI call reaches the resolver instead of pre-filling the maintainer's path. The explicit `memory_root` parameter override and the fail-soft `OSError` return are unchanged (both covered by new tests).

**`scripts/transcript_pattern_miner/__main__.py`** (critical). Added `pro
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
