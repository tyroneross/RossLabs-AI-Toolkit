---
id: 847aae77169f5b7a
kind: correction
signal_type: stop_directive
confidence: confirmed
scope: global
turn_index: 29
captured_chars: 20
tier: 1-deterministic
source: stop-hook
title: 'stop being invisible'
captured_at: 2026-08-19T05:11:20.811430+00:00
extras:
  prior_assistant_acted: false
---

## Quote

> stop being invisible

## Context (±200 chars)

```
t sentence plus any sentence starting `NOT`/`Distinct from`). Also make `build_row()` fall back to `when_to_use:` when `description:` lacks trigger language, so the auto-capture skills' real triggers stop being invisible.
2. **Fix the three genuine gaps at the source**, one sentence each: add a `NOT for X, use research plugin` line to `skills/research/SKILL.md:3`; add mutual boundary sentences to `self-improve`/`recu
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
