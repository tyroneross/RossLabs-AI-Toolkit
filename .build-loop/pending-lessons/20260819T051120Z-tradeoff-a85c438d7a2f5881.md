---
id: a85c438d7a2f5881
kind: tradeoff
signal_type: instead_of
confidence: confirmed
scope: project
turn_index: 25
captured_chars: 49
tier: 1-deterministic
source: stop-hook
title: 'but tracks the declaration instead of freezing it'
captured_at: 2026-08-19T05:11:20.809107+00:00
---

## Quote

> but tracks the declaration instead of freezing it

## Context (±200 chars)

```
d documents `.build-loop/backlog/items/`. The test held a literal copy of the pre-backlog lane set. Rewrote it as `set(rbk.keys()) == set(KINDS)` — still catches both a dropped lane and a leaked key, but tracks the declaration instead of freezing it.

→ note: the same frozen literal existed in **production** code at `scripts/audit_memory_invocation.py:179`, where it computes `missing_kinds`. That probe would have reported `verdict: ok` with the
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
