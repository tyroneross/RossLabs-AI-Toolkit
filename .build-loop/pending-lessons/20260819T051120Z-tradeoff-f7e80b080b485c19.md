---
id: f7e80b080b485c19
kind: tradeoff
signal_type: instead_of
confidence: confirmed
scope: project
turn_index: 25
captured_chars: 99
tier: 1-deterministic
source: stop-hook
title: 'so it is restored per test instead of mutating the session environment the way t'
captured_at: 2026-08-19T05:11:20.809519+00:00
---

## Quote

> so it is restored per test instead of mutating the session environment the way the module-level `os

## Context (±200 chars)

```
n inventing one — `scripts/test_audit_git.py:31-35` documents this exact mechanism — but scoped it to an autouse `monkeypatch` fixture setting `GIT_CONFIG_GLOBAL=/dev/null` + `GIT_CONFIG_NOSYSTEM=1`, so it is restored per test instead of mutating the session environment the way the module-level `os.environ.setdefault` precedent does. Hermetic on a clean machine and on any CI runner that sets tag/commit signing or a commit template.

## Gates
- `check_portability.py --all` → exit 0
- `methodolog
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
