---
id: 050657ccf52c1217
kind: preference
signal_type: never
confidence: confirmed
scope: global
turn_index: 5
captured_chars: 18
tier: 1-deterministic
source: stop-hook
title: 'never sees (below)'
captured_at: 2026-08-19T05:11:20.804410+00:00
---

## Quote

> never sees (below)

## Context (±200 chars)

```
are private.

**5. The fix as scoped misses the two worst defects entirely** — they're in Python, not markdown (miner `SESSIONS_DIR`, `append_milestone.py` mkdir), and in path classes a `~/dev/` grep never sees (below). A markdown-only pass would let the maintainer declare the defect fixed while every fresh install still grows a phantom `~/dev/git-folder/build-loop-memory`.

**6. Where skip produces a silently wors
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
