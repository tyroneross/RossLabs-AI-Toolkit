---
id: ceec164e56a0e9e4
kind: preference
signal_type: always
confidence: confirmed
scope: global
turn_index: 29
captured_chars: 80
tier: 1-deterministic
source: stop-hook
title: 'always keep the first sentence plus any sentence starting `NOT`/`Distinct from`)'
captured_at: 2026-08-19T05:11:20.811746+00:00
---

## Quote

> always keep the first sentence plus any sentence starting `NOT`/`Distinct from`)

## Context (±200 chars)

```
.py`: raise `DESCRIPTION_MAX` (line 70/169) well past 160 — Fable's point stands, a markdown table cell costs nothing for width, only human aesthetics — or make `truncate()` disambiguator-preserving (always keep the first sentence plus any sentence starting `NOT`/`Distinct from`). Also make `build_row()` fall back to `when_to_use:` when `description:` lacks trigger language, so the auto-capture skills' real triggers stop being invisible.
2. **Fix the three genuine gaps at the
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
