---
id: aeac37ac810a6b0f
kind: preference
signal_type: must
confidence: confirmed
scope: global
turn_index: 30
captured_chars: 53
tier: 1-deterministic
source: stop-hook
title: 'must appear verbatim in the rendered row (fails today'
captured_at: 2026-08-19T05:11:20.815556+00:00
---

## Quote

> must appear verbatim in the rendered row (fails today

## Context (±200 chars)

```
oes.
2. **Standardize** — fold `when_to_use:` into `description:` so there's one field the generator can't miss.
3. **Automate detection** — a new test: every `NOT for` clause in a source description must appear verbatim in the rendered row (fails today, 11/11); a pairwise collision check across shared trigger phrases (fails today on the 3 content-level gaps: research vs. research-plugin, self-improve vs. recursive-retrospective, architecture-rules
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
