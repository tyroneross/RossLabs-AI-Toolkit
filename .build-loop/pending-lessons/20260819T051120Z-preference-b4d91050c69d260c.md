---
id: b4d91050c69d260c
kind: preference
signal_type: never
confidence: confirmed
scope: global
turn_index: 31
captured_chars: 40
tier: 1-deterministic
source: stop-hook
title: 'never reads (`when_to_use` in build-loop'
captured_at: 2026-08-19T05:11:20.816489+00:00
---

## Quote

> never reads (`when_to_use` in build-loop

## Context (±200 chars)

```
-Toolkit's `showcase-awareness`. One of these (`showcase-awareness`) is the same root-cause shape as this session's RCA finding — real trigger content sitting in a frontmatter field the host silently never reads (`when_to_use` in build-loop, `triggers:` there) — confirmed independently in a second, unrelated repo. That's no longer a one-off; it's a pattern worth a lint rule, which I logged in the decision record rather than acting on un
```

## Next action for the host agent

Refine this candidate into a durable record. Decide kind (decision|lesson|feedback) and scope (project|global). If aligned with build-loop-memory contract, promote via `scripts/memory_writer.py` or `scripts/write_decision/__main__.py`. If not actionable, delete this file.
