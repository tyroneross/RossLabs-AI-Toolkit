# Orphan plugins archived 2026-04-21

Moved out of `plugins/` because they are not registered in `.claude-plugin/marketplace.json`.

- **gator/** — duplicate of `navgator/`. The marketplace publishes `navgator` (source: `tyroneross/NavGator`); this tree was a stale second copy under the old `gator` name.
- **plugin-sync/** — internal maintenance utility, never exposed in the marketplace. Kept here for reference.

If either is revived, restore to `plugins/` and add a corresponding entry to `.claude-plugin/marketplace.json`.
